# SSH server / workstation paths. Symlink to activate:  ln -sf .env.server .env
PROJECT_ROOT=~/workspace/projects/longhorizon
# Raw media often sits on a separate disk on the server — set it here if so:
# RAW_DIR=/mnt/data/longhorizon/raw
